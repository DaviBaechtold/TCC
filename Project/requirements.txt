# NOTE: The following are Python packages. On Linux you may also want to install
# system packages for video device helpers and OpenCV GUI support:
#   sudo apt update && sudo apt install -y v4l-utils libgl1 libglib2.0-0
# `v4l-utils` provides `v4l2-ctl --list-devices` which is useful to inspect
# camera nodes; `libgl1`/`libglib2.0-0` are commonly needed for OpenCV GUI windows.

# Core
numpy<2
pandas>=1.5
scikit-learn>=1.1
matplotlib>=3.6
seaborn>=0.12
opencv-python>=4.7
mediapipe>=0.10.14
# Deep Learning
torch>=2.2
torchvision>=0.17
# Utils
pyyaml>=6.0
tqdm>=4.66
rich>=13.7
