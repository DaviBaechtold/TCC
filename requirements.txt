# Dependências de execução, com as versões verificadas numa instalação limpa.
#
# O PyTorch NÃO está aqui: ele precisa vir do índice com CUDA 12.8, o único que
# suporta as GPUs da série RTX 50, e o pip não escolhe índice por pacote.
# Instale-o antes, como descrito no README:
#   pip install torch==2.8.0 torchvision==0.23.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# O MMCV entra como `mmcv-lite`, a variante sem operadores compilados: o MMCV
# completo não tem wheel para o PyTorch 2.8, e compilá-lo exige o CUDA Toolkit.
# O projeto não usa nenhum operador no caminho de inferência, e
# `src/models/mmcv_ops_fallback.py` supre a supressão de caixas que o detector
# RTMDet pediria. Ela e o chumpy têm `setup.py` antigos, que importam
# `pkg_resources`: instale-os antes, com `setuptools<81` e sem isolamento de
# build, como no README.

numpy==2.1.2
opencv-python==4.12.0.88
tqdm==4.65.2
matplotlib==3.10.6
scipy==1.16.2
albumentations==1.4.24

# Ecossistema OpenMMLab. O MMPose 1.3.2 exige MMDet < 3.3 e MMCV < 2.2.
mmengine==0.10.7
mmcv-lite==2.1.0
mmdet==3.2.0
mmpose==1.3.2
xtcocotools==1.14.3
json-tricks==3.17.3
munkres==1.1.4
chumpy==0.70

# Detector de pessoas de operação (YOLO26n-pose).
ultralytics==8.4.147

# Análise estática usada na revisão do código.
pyflakes==3.4.0
